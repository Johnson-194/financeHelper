import SwiftUI
import SwiftData

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Finance Tracker")
                .font(.largeTitle)
                .fontWeight(.bold)
            
            HStack {
                Button("Edit Data") {
                    
                }
                Button("View Summary") {
                    
                }
            }
        }
    }
}

#Preview {
    ContentView()
}
